{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [{
        "taskName": "Run Build Task",
        "command": "python",
        "args": [
            "${file}"
        ],
        "options": {
            "env": {
                "PYTHONPATH": "z:/Epoc32/winscw/c/resource"
            }
        },
        "type": "shell",
        "group": {
            "kind": "build",
            "isDefault": true
        }

    }]
}